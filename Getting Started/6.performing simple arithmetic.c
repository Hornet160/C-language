#include<stdio.h>
int main(void)
{
    printf("Dear Procrastinator,\nyou still have to wait for %d days (%d minutes or %d seconds) before you can procrastinate!", 2, (60*24*2), (60*(60*24*2)));
    return 0;
}
